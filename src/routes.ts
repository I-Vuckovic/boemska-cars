export const baseURL = `http://localhost:1357`;

export const cars = `${baseURL}/cars/`

export const like = `${baseURL}/like/`;

export const dislike = `${baseURL}/dislike/`;

export const login = `${baseURL}/login`;

export const register = `${baseURL}/register`;